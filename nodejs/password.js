module.exports.HashPassword = function (password)
{
        //let us assume that password is Hashed
        return "abra ka dabra";
}
module.exports.ComparePasswords = function(PlainTextPassword,HashedPassword)
{
        //let us assume both password are compared and matched successfully
        return true;
}
